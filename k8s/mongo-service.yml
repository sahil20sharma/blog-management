apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
spec:
  selector:
    app: mongo #) uss pod se traffic bhejegi jiska label mongo hoga
  ports:
  - port: 27017
    targetPort: 27017
    
